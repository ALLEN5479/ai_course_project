<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.ai_lesson.mission.Mapper.MissionStudentScoreMapper">
    <insert id="insertScore">
        INSERT INTO mission_student_score (mission_id, student_id, score)
        VALUES (#{missionId}, #{studentId}, #{score})
    </insert>
</mapper> 